# 🐱 Cat Meme Picker

A fun mini-project from the Scrimba Fullstack Path. It lets users pick cat memes by emotion or show only GIFs.

## 🧠 What It Does
- Displays different cat memes based on user selections.
- Filters memes by emotion and GIF preference.
- Dynamically updates the page using vanilla JavaScript.

## 💻 Built With
- HTML  
- CSS  
- JavaScript (ES6+)

## 🔑 Key JavaScript Methods
| Method | What It Does |
|--------|---------------|
| `forEach()` | Loops through the list of cat memes to create and display radio buttons or images. |
| `filter()` | Filters memes by selected emotion or whether they’re GIFs. |
| `find()` | Finds a meme object that matches the chosen criteria. |
| `addEventListener()` | Handles user clicks to show or close the meme modal. |
| `DOM methods` (`querySelector`, `innerHTML`, `classList`) | Used to update and manipulate the page content dynamically. |

## 🚀 How to Run
1. Clone this repo:  
   ```bash
   git clone https://github.com/Yusuph-Darbo/scrimba-fullstack-path.git
