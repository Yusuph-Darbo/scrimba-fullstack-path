# 🧭 Lead Tracker

A simple **Chrome extension** that helps users save and manage leads (URLs) directly from their browser.

---

## 🚀 Features

* Save the current browser tab with one click
* Add custom URLs manually
* View all saved leads in a clean list
* Delete all leads with a single button
* Data persistence using **localStorage**

---

## 🛠️ Built With

* **HTML5**
* **CSS3**
* **Vanilla JavaScript (ES6)**

---

## 📦 Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/Yusuph-Darbo/scrimba-fullstack-path.git
   cd 02-javascript/projects/leadTracker
   ```

2. Open `index.html` in your browser, **or**
   install it as a **Chrome extension**:

   * Open Chrome → `chrome://extensions/`
   * Enable **Developer mode**
   * Click **Load unpacked**
   * Select the `leadTracker` folder

---

## 💡 Usage

* **SAVE INPUT:** Enter a URL and click “Save Input” to add it to your list
* **SAVE TAB:** Click “Save Tab” to automatically add the active tab
* **DELETE ALL:** Double-click the “Delete All” button to clear all leads
